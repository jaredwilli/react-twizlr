

```js
<div className="close-buttons">
    <CloseButton
        onClick={() => buttonClicked()} />

    <CloseButton
        text="Close"
        onClick={() => buttonClicked()} />

    <CloseButton
        icon="check"
        text="Done"
        onClick={() => buttonClicked()} />
</div>
```
